#include <Graph.h>




void Graph::addVertex(const V vIndex, const V vNew)
{
    VList::iterator it = _vertexs.find(vIndex);
    if(it == _vertexs.end())
    {
        _vertexs.push_back()

    }



}

void Graph::addEdge(Edge edge)
{
    start = edge.start();


}
